const login = require("./_login");
const add = require("./_add");
const list = require("./_list");
const show = require("./_show");
const remove = require("./_remove");
const unremove = require("./_unremove");
const edit = require("./_edit");
const off_active = require("./_off-active");
const list_active = require("./_list-active");
const list_blocked = require("./_list-bloacked");

module.exports = {
  login,
  add,
  list,
  show,
  remove,
  unremove,
  edit,
  off_active,
  list_active,
  list_blocked,
};
