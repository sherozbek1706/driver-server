const db = require("../../../db");
const {
  NotFoundError,
  ForbiddenError,
  BadRequestError,
} = require("../../../shared/errors");

const handover = async ({ params, user }) => {
};

module.exports = handover;
